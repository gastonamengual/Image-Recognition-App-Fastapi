{
    "name": "image-recognition-api",
    "builds": [
        {
            "src": "server/main.py",
            "use": "@now/python"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "server/main.py"
        },
        {
            "src": "/detect_object",
            "dest": "api/api.py"
        }
    ]
}
